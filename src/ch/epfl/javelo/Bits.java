package ch.epfl.javelo;

/**
 * 2.3.3
 * Bits
 *
 * Classe contenant deux méthodes permettant d'extraire une séquence de bits d'un vecteur de 32 bits.
 *
 * @author Jean Nordmann (344692)
 * @author Maxime Ducourau (329544)
 */

public final class Bits {

    /**
     * Constructeur privé, car cette classe n'est pas instantiable.
     */

    private Bits() {}

    /*int extractSigned(int value, int start, int length) {
        String bit = Integer.toString(value);
        int leftShift = bit.substring(31 - start - length, 31);
        leftShift = "hahah";
    }*/



}
